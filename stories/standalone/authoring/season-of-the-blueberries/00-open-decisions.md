# Expansion Pack open decisions

These decisions require owner selection or visual review. They do not block
shipping an episode with the unresolved treatment omitted.

## 01:28 model plate

Add a Destiny-style lower third whose rows are factual Hugging Face model-card
statistics. The recommended current consumer model is
`unsloth/Qwen3.8-27B-GGUF`, backed by `Qwen/Qwen3.8-27B`: 27B parameters,
262,144-token native context (extensible to 1,000,000), Apache-2.0, and a GGUF
distribution with 8.4M downloads when researched. Alternatives considered:
`unsloth/Qwen3.8-Flash-Next-GGUF` and
`unsloth/GLM-5.3-Flash-GGUF`.

Freeze the selected repository and captured statistics in the episode record;
never query live model popularity during a render.

## Final CTA seat

The copy is in [final-cta.md](final-cta.md). Decide whether it closes every
weekly episode or only the full-season cut. The current delivered files keep
the existing training CTA until this is decided.

## Portrait and casting decisions

- Verify the CNCF staff-photo asset and permission for `jbryce` and
  `caniszczyk`.
- Approve the one-video role treatment pairing the real GitHub handles
  `castrojo` and `joannalee333` with Saint-14 and Anna Bray portraits.
- Cast Immaru at 15:43 or render the card without a real-person identity.
- Confirm whether the unmarked “I hate this job” line remains with `angellk`.
