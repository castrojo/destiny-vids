# These files name real people or carry rights decisions, so the owner
# reviews every change to them (AGENTS.md: casting names real people, and
# the rights posture is what keeps the project shippable).

/AGENTS.md           @castrojo
/vocab/casting.yaml  @castrojo
/dialogue/           @castrojo
/redactions/         @castrojo

# Tags decide two things nothing downstream can second-guess: `overlays`,
# which is the input to the `clean` gate, and `character`, which routes a real
# person's credit onto a shot. They are produced in volume by agents, so they
# are exactly the files where a wrong value arrives quietly.
/tags/               @castrojo
